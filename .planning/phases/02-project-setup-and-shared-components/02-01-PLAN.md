---
phase: 02-project-setup-and-shared-components
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - next.config.ts
  - postcss.config.mjs
  - tsconfig.json
  - .env.local
  - app/globals.css
  - app/layout.tsx
  - app/page.tsx
autonomous: true
requirements:
  - REQ-002

must_haves:
  truths:
    - "npm run build completes with zero TypeScript or ESLint errors"
    - "npm run dev starts a server at localhost:3000 without crashing"
    - "The page at localhost:3000 renders with fort-charcoal background on body and the Playfair Display + Inter fonts loaded"
    - "Phase 1 files (tailwind.config.js, app/fonts.ts, public/logo/) are untouched"
  artifacts:
    - path: "package.json"
      provides: "Next.js 15, React 19, TypeScript, Tailwind, motion, @formspree/react dependencies + dev scripts"
    - path: "next.config.ts"
      provides: "TypeScript Next.js config with image optimization"
    - path: "postcss.config.mjs"
      provides: "Tailwind + Autoprefixer pipeline"
    - path: "tsconfig.json"
      provides: "TypeScript strict config for Next.js App Router"
    - path: ".env.local"
      provides: "NEXT_PUBLIC_FORMSPREE_ENDPOINT placeholder"
    - path: "app/globals.css"
      provides: "@tailwind directives so fort-* tokens render"
    - path: "app/layout.tsx"
      provides: "Root layout: font variables on <html>, Nav + Footer wrappers, globals.css import"
    - path: "app/page.tsx"
      provides: "Home page placeholder enabling npm run build to pass"
  key_links:
    - from: "app/layout.tsx"
      to: "app/fonts.ts"
      via: "named imports { inter, playfair }"
      pattern: "import.*inter.*playfair.*from.*fonts"
    - from: "app/layout.tsx"
      to: "app/globals.css"
      via: "import './globals.css'"
      pattern: "import.*globals\\.css"
    - from: "tailwind.config.js"
      to: "app/globals.css"
      via: "@tailwind directives processed by postcss"
      pattern: "@tailwind (base|components|utilities)"
---

<objective>
Bootstrap the Next.js project into the existing repo without destroying Phase 1 brand files.

Purpose: The repo already has tailwind.config.js, app/fonts.ts, public/logo/, and app/icon.svg from Phase 1. We need Next.js packages and the minimum required files (layout, globals, page, config) to turn this into a running Next.js app. Do NOT run create-next-app.

Output: A working Next.js 15 project that passes `npm run build` with fort-* color tokens and fonts active. Plans 02 and 03 (Nav, Footer, components) can then be executed.
</objective>

<execution_context>
@C:/Users/denni/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/denni/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Existing Phase 1 files — do not modify these -->

From app/fonts.ts:
```typescript
export const inter    // Inter, variable: '--font-inter'
export const playfair // Playfair_Display, variable: '--font-playfair'
```

From tailwind.config.js (existing, DO NOT MODIFY):
```javascript
// Colors: fort-charcoal #1A1A2E, fort-blue #1B3A6B, fort-gold #C4973A, fort-bg #F8F6F1, fort-gray #374151
// Fonts: font-sans = var(--font-inter), font-serif = var(--font-playfair)
// Content: ./app/**/*.{js,ts,jsx,tsx,mdx}, ./components/**/*.{js,ts,jsx,tsx,mdx}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install packages and write config files</name>
  <files>package.json, next.config.ts, postcss.config.mjs, tsconfig.json, .env.local</files>
  <action>
Install Next.js and all dependencies into the existing directory (DO NOT run create-next-app):

```bash
cd C:/Users/denni/.notebooklm-mcp-cli
npm install next@latest react@latest react-dom@latest
npm install motion @formspree/react
npm install -D typescript @types/react @types/react-dom @types/node tailwindcss postcss autoprefixer eslint eslint-config-next
```

Create or update package.json scripts section. If package.json already exists, preserve existing fields and add/update only the scripts block:

```json
{
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint"
  }
}
```

Create next.config.ts:
```typescript
import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  images: {
    // Add external image domains here when needed (Phase 3/4)
  },
}

export default nextConfig
```

Create postcss.config.mjs:
```javascript
const config = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
}
export default config
```

Create tsconfig.json (standard Next.js strict config):
```json
{
  "compilerOptions": {
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": true,
    "skipLibCheck": true,
    "strict": true,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "preserve",
    "incremental": true,
    "plugins": [{ "name": "next" }],
    "paths": { "@/*": ["./*"] }
  },
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"],
  "exclude": ["node_modules"]
}
```

Create .env.local:
```bash
# Formspree — replace xxx with 8-character form hash from formspree.io dashboard
NEXT_PUBLIC_FORMSPREE_ENDPOINT=xxx
```

CRITICAL: Do NOT modify tailwind.config.js, app/fonts.ts, public/logo/, or app/icon.svg. These are Phase 1 deliverables.
  </action>
  <verify>
    <automated>cd C:/Users/denni/.notebooklm-mcp-cli && node -e "const p = require('./package.json'); const deps = {...p.dependencies, ...p.devDependencies}; ['next','react','react-dom','motion','typescript','tailwindcss'].forEach(d => { if (!deps[d]) throw new Error('Missing: ' + d); }); console.log('All required packages present');"</automated>
  </verify>
  <done>package.json has next, react, react-dom, motion, @formspree/react, typescript, tailwindcss, postcss, autoprefixer in dependencies/devDependencies. Scripts: dev, build, start, lint. next.config.ts, postcss.config.mjs, tsconfig.json, and .env.local all exist.</done>
</task>

<task type="auto">
  <name>Task 2: Create globals.css, layout.tsx, and page.tsx placeholder</name>
  <files>app/globals.css, app/layout.tsx, app/page.tsx</files>
  <action>
Create app/globals.css with Tailwind directives:
```css
@tailwind base;
@tailwind components;
@tailwind utilities;
```

Create app/layout.tsx — root layout that:
- Imports inter and playfair from './fonts' (the Phase 1 file)
- Imports Nav from '@/components/Nav' (will be created in Plan 02 — add a stub import guard or use dynamic import to avoid build failure)
- Imports Footer from '@/components/Footer' (will be created in Plan 03)
- Applies inter.variable and playfair.variable to <html> as className
- Sets body className to "bg-fort-bg text-fort-gray font-sans"
- Imports './globals.css'

IMPORTANT: Plans 02 and 03 create Nav and Footer. To allow this plan to build independently, create stub files now:
- Create components/Nav.tsx as a minimal stub: `export default function Nav() { return <header /> }`
- Create components/Footer.tsx as a minimal stub: `export default function Footer() { return <footer /> }`
These stubs will be fully replaced in Plans 02 and 03.

Full app/layout.tsx:
```typescript
import type { Metadata } from 'next'
import { inter, playfair } from './fonts'
import Nav from '@/components/Nav'
import Footer from '@/components/Footer'
import './globals.css'

export const metadata: Metadata = {
  title: 'Fort Property Developments',
  description: 'Multiplex infill developer serving Metro Vancouver and the Fraser Valley.',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" className={`${inter.variable} ${playfair.variable}`}>
      <body className="bg-fort-bg text-fort-gray font-sans">
        <Nav />
        <main>{children}</main>
        <Footer />
      </body>
    </html>
  )
}
```

Create app/page.tsx — minimal placeholder that satisfies the build:
```typescript
export default function Home() {
  return (
    <div className="min-h-screen flex items-center justify-center">
      <h1 className="font-serif text-4xl text-fort-charcoal">
        Fort Property Developments
      </h1>
    </div>
  )
}
```

Create components/ directory if it does not exist. Add the Nav and Footer stubs described above.
  </action>
  <verify>
    <automated>cd C:/Users/denni/.notebooklm-mcp-cli && npm run build 2>&1</automated>
  </verify>
  <done>`npm run build` exits with code 0 (zero TypeScript/ESLint errors). app/globals.css, app/layout.tsx, app/page.tsx all exist. components/Nav.tsx and components/Footer.tsx stubs exist. tailwind.config.js is unchanged (still contains fort-* color tokens).</done>
</task>

</tasks>

<verification>
After both tasks:
- `npm run build` passes with zero errors
- `npm run dev` starts without crashing (manual check: open localhost:3000)
- tailwind.config.js still contains fort-charcoal, fort-blue, fort-gold, fort-bg, fort-gray
- app/fonts.ts still exports inter and playfair unchanged
- public/logo/fort-logo-reversed.svg and fort-logo-primary.svg still exist
</verification>

<success_criteria>
- npm run build exits 0
- All 5 fort-* color tokens still present in tailwind.config.js
- Root layout applies font CSS variables to <html>
- Stub Nav and Footer components allow build to pass without Plan 02/03 being complete
</success_criteria>

<output>
After completion, create `.planning/phases/02-project-setup-and-shared-components/02-01-SUMMARY.md`
</output>
