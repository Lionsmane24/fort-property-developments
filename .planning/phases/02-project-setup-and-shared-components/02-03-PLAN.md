---
phase: 02-project-setup-and-shared-components
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - components/Footer.tsx
  - components/Button.tsx
  - components/Section.tsx
  - components/animations/FadeIn.tsx
  - components/animations/FadeInGroup.tsx
autonomous: false
requirements:
  - REQ-003

must_haves:
  truths:
    - "Footer renders on every page with logo, nav links, contact info, Privacy Policy link, and copyright with the current year"
    - "Button component renders primary (gold fill) and secondary (gold outline) variants"
    - "Section component wraps content in a max-width container with consistent padding"
    - "FadeIn component fades children from opacity 0 to 1 when they enter the viewport"
    - "FadeIn skips animation (renders at full opacity immediately) when prefers-reduced-motion is set"
    - "FadeInGroup staggers its children with 100ms delay between each child"
  artifacts:
    - path: "components/Footer.tsx"
      provides: "Server component: logo, nav links, contact info, privacy link, dynamic copyright year"
      exports: ["default Footer"]
    - path: "components/Button.tsx"
      provides: "Server component: primary and secondary button/link variants"
      exports: ["Button"]
    - path: "components/Section.tsx"
      provides: "Server component: max-width wrapper with py-16/py-24 padding"
      exports: ["Section"]
    - path: "components/animations/FadeIn.tsx"
      provides: "Client component: viewport-triggered opacity fade using motion/react whileInView"
      exports: ["FadeIn"]
    - path: "components/animations/FadeInGroup.tsx"
      provides: "Client component: staggered FadeIn children at 100ms intervals"
      exports: ["FadeInGroup"]
  key_links:
    - from: "components/animations/FadeIn.tsx"
      to: "motion/react"
      via: "import { motion, useReducedMotion } from 'motion/react'"
      pattern: "from 'motion/react'"
    - from: "components/animations/FadeInGroup.tsx"
      to: "components/animations/FadeIn.tsx"
      via: "import { FadeIn } from './FadeIn'"
      pattern: "from.*FadeIn"
    - from: "app/layout.tsx"
      to: "components/Footer.tsx"
      via: "import Footer from '@/components/Footer'"
      pattern: "import Footer"
---

<objective>
Build Footer, Button, Section (server components) and FadeIn, FadeInGroup (client animation utilities).

Purpose: These components are the building blocks for all 4 pages in Phase 3. Footer must replace the Plan 01 stub so the layout is complete. Animation components must be ready for use throughout the site.

Output: 5 components. Plans 02 and 03 both run in Wave 2 (parallel with Nav). Footer replaces its stub. FadeIn and FadeInGroup are ready for Phase 3.
</objective>

<execution_context>
@C:/Users/denni/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/denni/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-project-setup-and-shared-components/02-CONTEXT.md
@.planning/phases/02-project-setup-and-shared-components/02-01-SUMMARY.md

<interfaces>
<!-- Brand tokens available (from tailwind.config.js — do not modify) -->
<!-- fort-charcoal #1A1A2E, fort-blue #1B3A6B, fort-gold #C4973A, fort-bg #F8F6F1, fort-gray #374151 -->
<!-- font-sans = Inter, font-serif = Playfair Display -->

<!-- Logo for footer (dark background): /logo/fort-logo-reversed.svg -->
<!-- RULE: Never use raw hex in className — always use fort-* tokens -->

<!-- Scroll animation decisions (locked in CONTEXT.md):
  - Fade-in only: opacity 0 → 1
  - Duration: 400–600ms ease-out (use 0.5s)
  - Trigger: whileInView, once: true
  - Reduced motion: useReducedMotion() from motion/react — skip animation, render at full opacity
  - Card stagger: 100ms (0.1s) between each child in FadeInGroup
  - Import from 'motion/react' (NOT framer-motion)
  - All motion files MUST have 'use client' as first line
-->

<!-- Footer content requirements (from REQ-003):
  - Logo (reversed SVG, links to /)
  - Nav links: Home, Projects, About, Contact
  - Contact info: email + phone (use placeholders — real values provided in Phase 3)
  - Privacy Policy link (/privacy)
  - Copyright with dynamic year: new Date().getFullYear()
  - Service area text
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Footer.tsx, Button.tsx, and Section.tsx</name>
  <files>components/Footer.tsx, components/Button.tsx, components/Section.tsx</files>
  <action>
Replace the Plan 01 Footer stub and create Button and Section components.

**components/Footer.tsx** — Server component (no 'use client'):
```typescript
import Image from 'next/image'
import Link from 'next/link'

const footerLinks = [
  { label: 'Home', href: '/' },
  { label: 'Projects', href: '/projects' },
  { label: 'About', href: '/about' },
  { label: 'Contact', href: '/contact' },
  { label: 'Privacy Policy', href: '/privacy' },
]

export default function Footer() {
  const year = new Date().getFullYear()

  return (
    <footer className="bg-fort-charcoal text-white">
      <div className="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 py-12 md:py-16">
        <div className="grid grid-cols-1 md:grid-cols-3 gap-8 md:gap-12">
          {/* Column 1: Logo + tagline */}
          <div className="flex flex-col gap-4">
            <Link href="/" aria-label="Fort Property Developments — Home">
              <Image
                src="/logo/fort-logo-reversed.svg"
                alt="Fort Property Developments"
                width={160}
                height={40}
              />
            </Link>
            <p className="text-sm text-gray-400 leading-relaxed">
              Multiplex infill development in Metro Vancouver, the Fraser Valley, and the North Shore.
            </p>
          </div>

          {/* Column 2: Navigation */}
          <div className="flex flex-col gap-3">
            <h3 className="text-xs font-semibold uppercase tracking-wider text-fort-gold">
              Navigation
            </h3>
            <nav aria-label="Footer navigation">
              <ul className="flex flex-col gap-2">
                {footerLinks.map((link) => (
                  <li key={link.href}>
                    <Link
                      href={link.href}
                      className="text-sm text-gray-300 hover:text-white transition-colors duration-200"
                    >
                      {link.label}
                    </Link>
                  </li>
                ))}
              </ul>
            </nav>
          </div>

          {/* Column 3: Contact */}
          <div className="flex flex-col gap-3">
            <h3 className="text-xs font-semibold uppercase tracking-wider text-fort-gold">
              Contact
            </h3>
            <div className="flex flex-col gap-2 text-sm text-gray-300">
              {/* Real email + phone provided by Dennis in Phase 3 */}
              <a href="mailto:info@fortpropertydev.com" className="hover:text-white transition-colors duration-200">
                info@fortpropertydev.com
              </a>
              <a href="tel:+16045550000" className="hover:text-white transition-colors duration-200">
                (604) 555-0000
              </a>
              <p className="text-gray-400 mt-2">
                Serving Metro Vancouver · Fraser Valley · North Shore · Tri-Cities
              </p>
            </div>
          </div>
        </div>

        {/* Bottom bar */}
        <div className="mt-10 pt-8 border-t border-white/10 flex flex-col sm:flex-row items-center justify-between gap-4 text-xs text-gray-500">
          <p>&copy; {year} Fort Property Developments. All rights reserved.</p>
          <p>BC Real Estate Developer</p>
        </div>
      </div>
    </footer>
  )
}
```

**components/Button.tsx** — Server component (no 'use client'):
```typescript
interface ButtonProps {
  variant?: 'primary' | 'secondary'
  href?: string
  children: React.ReactNode
  className?: string
  disabled?: boolean
  type?: 'button' | 'submit' | 'reset'
}

export function Button({
  variant = 'primary',
  href,
  children,
  className,
  disabled,
  type = 'button',
}: ButtonProps) {
  const base =
    'inline-flex items-center justify-center px-6 py-3 rounded font-sans font-semibold text-sm transition-colors duration-200 focus:outline-none focus-visible:ring-2 focus-visible:ring-fort-gold focus-visible:ring-offset-2'
  const variants = {
    primary: 'bg-fort-gold text-white hover:bg-amber-600 disabled:opacity-50',
    secondary:
      'border-2 border-fort-gold text-fort-gold hover:bg-fort-gold hover:text-white disabled:opacity-50',
  }
  const classes = `${base} ${variants[variant]} ${className ?? ''}`

  if (href) {
    return (
      <a href={href} className={classes}>
        {children}
      </a>
    )
  }
  return (
    <button type={type} disabled={disabled} className={classes}>
      {children}
    </button>
  )
}
```

**components/Section.tsx** — Server component (no 'use client'):
```typescript
interface SectionProps {
  children: React.ReactNode
  className?: string
  id?: string
}

export function Section({ children, className, id }: SectionProps) {
  return (
    <section id={id} className={`w-full py-16 md:py-24 ${className ?? ''}`}>
      <div className="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
        {children}
      </div>
    </section>
  )
}
```
  </action>
  <verify>
    <automated>cd C:/Users/denni/.notebooklm-mcp-cli && npm run build 2>&1</automated>
  </verify>
  <done>npm run build passes. components/Footer.tsx, components/Button.tsx, and components/Section.tsx all exist. Footer is not the stub (contains copyright year logic, nav links, logo image). No raw hex values in any component className.</done>
</task>

<task type="auto">
  <name>Task 2: Create FadeIn.tsx and FadeInGroup.tsx animation utilities</name>
  <files>components/animations/FadeIn.tsx, components/animations/FadeInGroup.tsx</files>
  <action>
Create the components/animations/ directory and both animation components.

CRITICAL:
- Both files MUST have 'use client' as the very first line — motion/react requires client-side rendering
- Import from 'motion/react' NOT 'framer-motion'
- useReducedMotion() is from motion/react — do not hand-roll matchMedia

**components/animations/FadeIn.tsx**:
```typescript
'use client'

import { motion, useReducedMotion } from 'motion/react'

interface FadeInProps {
  children: React.ReactNode
  className?: string
  delay?: number
}

export function FadeIn({ children, className, delay = 0 }: FadeInProps) {
  const shouldReduce = useReducedMotion()

  return (
    <motion.div
      className={className}
      initial={{ opacity: shouldReduce ? 1 : 0 }}
      whileInView={{ opacity: 1 }}
      viewport={{ once: true, amount: 'some' }}
      transition={{ duration: 0.5, ease: 'easeOut', delay }}
    >
      {children}
    </motion.div>
  )
}
```

**components/animations/FadeInGroup.tsx**:
```typescript
'use client'

import { FadeIn } from './FadeIn'

interface FadeInGroupProps {
  children: React.ReactNode[]
  className?: string
}

export function FadeInGroup({ children, className }: FadeInGroupProps) {
  return (
    <div className={className}>
      {children.map((child, i) => (
        <FadeIn key={i} delay={i * 0.1}>
          {child}
        </FadeIn>
      ))}
    </div>
  )
}
```

Note: 100ms stagger = 0.1 seconds. First child: delay=0, second: delay=0.1, third: delay=0.2, etc.
  </action>
  <verify>
    <automated>cd C:/Users/denni/.notebooklm-mcp-cli && npm run build 2>&1</automated>
  </verify>
  <done>npm run build passes. components/animations/FadeIn.tsx and components/animations/FadeInGroup.tsx exist. Both files have 'use client' as the first line. Both import from 'motion/react' (not 'framer-motion'). FadeIn uses useReducedMotion() — when true, initial opacity is 1 (no animation). FadeInGroup maps children with i * 0.1 delay.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Footer (logo, nav links, contact info, privacy link, dynamic copyright year), Button component (primary/secondary variants), Section component (max-width wrapper), FadeIn and FadeInGroup animation utilities.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:3000
    2. Scroll to the bottom — confirm footer is visible with logo, navigation links, contact placeholders, and "© 2026 Fort Property Developments"
    3. Confirm Privacy Policy link appears in the footer navigation list
    4. Open browser DevTools → Console — confirm no errors related to motion, hydration, or missing modules
    5. Optional: Temporarily add `&lt;FadeIn&gt;&lt;p&gt;Test&lt;/p&gt;&lt;/FadeIn&gt;` to app/page.tsx, save, and confirm the text fades in on scroll (then revert)
    6. Run `npm run build` — confirm it passes with zero errors
  </how-to-verify>
  <resume-signal>Type "approved" to mark Phase 2 complete, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
After all tasks:
- `npm run build` passes with zero errors
- Footer visible at localhost:3000 with current year, logo, links
- No 'framer-motion' in any import (only 'motion/react')
- No raw hex values in component classNames
- All 5 files exist: Footer.tsx, Button.tsx, Section.tsx, animations/FadeIn.tsx, animations/FadeInGroup.tsx
</verification>

<success_criteria>
- Footer renders on the page with logo, nav links, contact info, Privacy Policy link, dynamic copyright year
- Button and Section components are importable by Phase 3 page files
- FadeIn and FadeInGroup are ready for use in Phase 3 with correct 100ms stagger and reduced-motion support
- npm run build exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/02-project-setup-and-shared-components/02-03-SUMMARY.md`
</output>
