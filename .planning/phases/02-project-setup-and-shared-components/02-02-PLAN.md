---
phase: 02-project-setup-and-shared-components
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - components/Nav.tsx
autonomous: false
requirements:
  - REQ-003

must_haves:
  truths:
    - "Nav is fixed to the top of the page (does not scroll away)"
    - "Nav background is always solid fort-charcoal — no transparency"
    - "A subtle drop shadow appears on the nav header after the user scrolls past 50px"
    - "Desktop nav shows: logo left, links (Home · Projects · About · Contact) right, 'Book a Call' gold CTA button"
    - "Mobile nav (≤768px) shows: logo left, hamburger icon right"
    - "Tapping the hamburger opens a full-screen overlay with centered links and CTA on fort-charcoal (#1A1A2E) background"
    - "Closing the overlay (X icon or link tap) restores normal page scroll"
    - "The overlay closes automatically when the route changes"
  artifacts:
    - path: "components/Nav.tsx"
      provides: "Sticky nav with scroll shadow, desktop links, mobile full-screen overlay"
      exports: ["default Nav"]
  key_links:
    - from: "components/Nav.tsx"
      to: "next/navigation"
      via: "usePathname() in useEffect to close overlay on route change"
      pattern: "usePathname"
    - from: "components/Nav.tsx"
      to: "public/logo/fort-logo-reversed.svg"
      via: "next/image with src='/logo/fort-logo-reversed.svg'"
      pattern: "fort-logo-reversed"
    - from: "app/layout.tsx"
      to: "components/Nav.tsx"
      via: "import Nav from '@/components/Nav'"
      pattern: "import Nav"
---

<objective>
Build the full Nav component: sticky with scroll shadow, desktop links, mobile full-screen overlay.

Purpose: Navigation is the primary wayfinding element on every page. It must work correctly at both 375px (mobile) and 1280px (desktop) before any page content is built.

Output: components/Nav.tsx replaces the Plan 01 stub. The nav is fully functional: sticky positioning, scroll shadow, desktop layout, mobile hamburger overlay with all locked decisions implemented.
</objective>

<execution_context>
@C:/Users/denni/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/denni/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-project-setup-and-shared-components/02-CONTEXT.md
@.planning/phases/02-project-setup-and-shared-components/02-01-SUMMARY.md

<interfaces>
<!-- Locked decisions from CONTEXT.md — implement exactly as specified -->

Nav links (in order): Home (/), Projects (/projects), About (/about), Contact (/contact)
- "Home" hidden on desktop (logo already links home), visible in mobile overlay

Nav CTA:
- Label: "Book a Call"
- href: "#booking"
- Style: bg-fort-gold text-white (gold button)

Scroll behavior:
- Position: fixed top-0 left-0 right-0 z-50
- Background: always bg-fort-charcoal (no transparency, no change on scroll)
- Shadow: shadow-lg after window.scrollY > 50, shadow-none before
- Transition: transition-shadow duration-300

Mobile overlay:
- Background: #1A1A2E (fort-charcoal hex)
- Links: centered, large, white text, vertically centered
- CTA: same "Book a Call" gold button
- Must close on: X button click, link tap, route change

Brand tokens available (from tailwind.config.js):
- fort-charcoal: #1A1A2E
- fort-blue:     #1B3A6B
- fort-gold:     #C4973A
- fort-bg:       #F8F6F1
- fort-gray:     #374151

Logo file: /logo/fort-logo-reversed.svg (white wordmark for dark nav background)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Nav.tsx with sticky scroll shadow and desktop layout</name>
  <files>components/Nav.tsx</files>
  <action>
Replace the Plan 01 stub at components/Nav.tsx with the full implementation.

This is a 'use client' component. It uses:
- useState for scrolled (scroll shadow) and menuOpen (mobile overlay)
- useEffect for scroll listener + body overflow lock + pathname watcher
- usePathname from next/navigation (close menu on route change)
- next/image for the logo SVG
- next/link for navigation links

CRITICAL RULES:
- Import from 'motion/react' is NOT used in this file (Nav handles its own scroll state)
- Do NOT set overflow: hidden on a parent element — it breaks position: fixed. Use document.body.style.overflow for the body scroll lock only.
- Always use fort-* Tailwind tokens. Never use raw hex values in className.
- The 'use client' directive must be the very first line.

Full implementation:

```typescript
'use client'

import { useState, useEffect } from 'react'
import { usePathname } from 'next/navigation'
import Image from 'next/image'
import Link from 'next/link'

const navLinks = [
  { label: 'Home', href: '/', mobileOnly: true },
  { label: 'Projects', href: '/projects', mobileOnly: false },
  { label: 'About', href: '/about', mobileOnly: false },
  { label: 'Contact', href: '/contact', mobileOnly: false },
]

export default function Nav() {
  const [scrolled, setScrolled] = useState(false)
  const [menuOpen, setMenuOpen] = useState(false)
  const pathname = usePathname()

  // Scroll shadow
  useEffect(() => {
    const handleScroll = () => setScrolled(window.scrollY > 50)
    window.addEventListener('scroll', handleScroll, { passive: true })
    return () => window.removeEventListener('scroll', handleScroll)
  }, [])

  // Close menu on route change
  useEffect(() => {
    setMenuOpen(false)
  }, [pathname])

  // Body scroll lock when overlay is open
  useEffect(() => {
    document.body.style.overflow = menuOpen ? 'hidden' : ''
    return () => { document.body.style.overflow = '' }
  }, [menuOpen])

  return (
    <>
      <header
        className={`fixed top-0 left-0 right-0 z-50 bg-fort-charcoal transition-shadow duration-300 ${
          scrolled ? 'shadow-lg' : 'shadow-none'
        }`}
      >
        <div className="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
          <div className="flex items-center justify-between h-16 md:h-20">
            {/* Logo */}
            <Link href="/" aria-label="Fort Property Developments — Home">
              <Image
                src="/logo/fort-logo-reversed.svg"
                alt="Fort Property Developments"
                width={160}
                height={40}
                priority
              />
            </Link>

            {/* Desktop nav */}
            <nav className="hidden md:flex items-center gap-8" aria-label="Main navigation">
              {navLinks
                .filter((link) => !link.mobileOnly)
                .map((link) => (
                  <Link
                    key={link.href}
                    href={link.href}
                    className="text-white font-sans text-sm font-medium hover:text-fort-gold transition-colors duration-200"
                  >
                    {link.label}
                  </Link>
                ))}
              <a
                href="#booking"
                className="inline-flex items-center justify-center px-5 py-2 rounded bg-fort-gold text-white font-sans text-sm font-semibold hover:bg-amber-600 transition-colors duration-200 focus:outline-none focus-visible:ring-2 focus-visible:ring-fort-gold focus-visible:ring-offset-2 focus-visible:ring-offset-fort-charcoal"
              >
                Book a Call
              </a>
            </nav>

            {/* Mobile hamburger */}
            <button
              className="md:hidden flex flex-col items-center justify-center w-10 h-10 gap-1.5 focus:outline-none focus-visible:ring-2 focus-visible:ring-fort-gold rounded"
              onClick={() => setMenuOpen(true)}
              aria-label="Open menu"
              aria-expanded={menuOpen}
            >
              <span className="block w-6 h-0.5 bg-white" />
              <span className="block w-6 h-0.5 bg-white" />
              <span className="block w-6 h-0.5 bg-white" />
            </button>
          </div>
        </div>
      </header>

      {/* Mobile full-screen overlay */}
      {menuOpen && (
        <div
          className="fixed inset-0 z-[60] bg-fort-charcoal flex flex-col items-center justify-center"
          role="dialog"
          aria-modal="true"
          aria-label="Navigation menu"
        >
          {/* Close button */}
          <button
            className="absolute top-5 right-4 w-10 h-10 flex items-center justify-center text-white focus:outline-none focus-visible:ring-2 focus-visible:ring-fort-gold rounded"
            onClick={() => setMenuOpen(false)}
            aria-label="Close menu"
          >
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round">
              <line x1="18" y1="6" x2="6" y2="18" />
              <line x1="6" y1="6" x2="18" y2="18" />
            </svg>
          </button>

          {/* Overlay links */}
          <nav className="flex flex-col items-center gap-8" aria-label="Mobile navigation">
            {navLinks.map((link) => (
              <Link
                key={link.href}
                href={link.href}
                className="text-white font-sans text-2xl font-medium hover:text-fort-gold transition-colors duration-200"
                onClick={() => setMenuOpen(false)}
              >
                {link.label}
              </Link>
            ))}
            <a
              href="#booking"
              className="mt-4 inline-flex items-center justify-center px-8 py-3 rounded bg-fort-gold text-white font-sans text-base font-semibold hover:bg-amber-600 transition-colors duration-200"
              onClick={() => setMenuOpen(false)}
            >
              Book a Call
            </a>
          </nav>
        </div>
      )}
    </>
  )
}
```
  </action>
  <verify>
    <automated>cd C:/Users/denni/.notebooklm-mcp-cli && npm run build 2>&1</automated>
  </verify>
  <done>npm run build passes. components/Nav.tsx exists as a full client component (not the stub). File contains: 'use client' as first line, usePathname, scroll shadow logic, mobile overlay with menuOpen state, body overflow lock, fort-logo-reversed.svg in next/image, all 4 nav links, "Book a Call" CTA targeting #booking.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Full Nav component: sticky charcoal header, scroll shadow at 50px, desktop links (Projects · About · Contact) + "Book a Call" gold button, mobile full-screen overlay with hamburger.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:3000
    2. Desktop (1280px): Confirm nav shows logo left, Projects/About/Contact links right, gold "Book a Call" button
    3. Desktop: Scroll past 50px — confirm a subtle shadow appears under the nav header
    4. Mobile (375px via Chrome DevTools → Toggle device toolbar): Confirm only logo and hamburger icon are visible
    5. Mobile: Tap hamburger — confirm full-screen dark overlay opens with Home/Projects/About/Contact centered + "Book a Call" gold button
    6. Mobile: Tap a link in the overlay — confirm overlay closes
    7. Mobile: Tap X button — confirm overlay closes and page scrolls normally again
  </how-to-verify>
  <resume-signal>Type "approved" to continue to Plan 03, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
- npm run build passes (zero TypeScript errors)
- Nav renders correctly at desktop and mobile widths
- All 7 manual checks in the checkpoint pass
</verification>

<success_criteria>
- Sticky charcoal nav visible on localhost:3000
- Drop shadow appears after 50px scroll
- Mobile hamburger opens full-screen overlay
- Overlay closes on link tap, X button, and route change
- No raw hex values in component — only fort-* tokens
- 'use client' is first line of Nav.tsx
</success_criteria>

<output>
After completion, create `.planning/phases/02-project-setup-and-shared-components/02-02-SUMMARY.md`
</output>
