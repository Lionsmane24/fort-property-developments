---
phase: 04-polish-and-launch
plan: 03
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - app/privacy/page.tsx
  - app/favicon.ico
  - app/icon.png
autonomous: true
requirements: [REQ-009, REQ-010]

must_haves:
  truths:
    - "Visiting /privacy returns a 200 with a Privacy Policy page (no 404)"
    - "Privacy Policy page has an h1 and readable content"
    - "Footer Privacy Policy link navigates to /privacy without error"
    - "Favicon is visible in the browser tab"
    - "favicon.ico and 192px icon.png exist in app/"
  artifacts:
    - path: "app/privacy/page.tsx"
      provides: "Static Privacy Policy route with metadata"
      contains: "Privacy Policy"
    - path: "app/favicon.ico"
      provides: "ICO favicon for browser tab and bookmarks"
    - path: "app/icon.png"
      provides: "192px PNG icon for PWA/Android home screen"
  key_links:
    - from: "components/Footer.tsx"
      to: "app/privacy/page.tsx"
      via: "href='/privacy' already in footer footerLinks array"
      pattern: "href.*privacy"
---

<objective>
Create the Privacy Policy page at /privacy and add favicon.ico + icon.png to the app/ directory. These are parallel to Plan 02 (SEO metadata) — both run in Wave 2, neither touches the same files.

Purpose: REQ-009 (PIPA BC legal requirement — privacy policy must exist before collecting personal data). REQ-010 partial (favicon files). Footer already links to /privacy; this plan makes the route real.
Output: app/privacy/page.tsx (static page), app/favicon.ico, app/icon.png.
</objective>

<execution_context>
@C:/Users/denni/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/denni/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

# Key context:

Footer (components/Footer.tsx) already has `href: '/privacy'` in its footerLinks array — confirmed in codebase. No footer changes needed. Just create the route.

Privacy Policy content: Dennis must generate this from TermsFeed (termsfeed.com) using:
- Company name: Fort Property Developments
- Email: Dennis@fortpropertydevelopment.com
- Website: www.FortPropertyDevelopments.com
- Jurisdiction: British Columbia, Canada

Until Dennis provides the generated content, create the page with a clear placeholder that builds and renders correctly. The route must exist — content can be swapped in after Dennis generates it.

Favicon source: public/logo/fort-logo-primary.svg (already exists in repo).
Next.js App Router file-based favicon API:
- app/favicon.ico → auto-served at /favicon.ico, auto-linked in <head>
- app/icon.png (192×192) → auto-served at /icon.png
- app/icon.svg already exists — continues serving as SVG favicon (no changes needed)

For generating favicon files: Use an online tool (favicon.io favicon converter or squoosh.app)
to convert public/logo/fort-logo-primary.svg to:
1. 32×32 favicon.ico
2. 192×192 icon.png

These are binary files — Claude should create placeholder text files at the correct paths
with instructions, and leave a clear note that Dennis needs to replace them with real binary
exports. Alternatively: use sharp/ImageMagick if available on the system to auto-generate.

Check if sharp is available: `npx sharp-cli --version 2>/dev/null`
Check if ImageMagick is available: `convert --version 2>/dev/null`
If neither available, create the placeholder file approach documented below.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Privacy Policy page at /privacy</name>
  <files>app/privacy/page.tsx</files>
  <action>
    Create `app/privacy/page.tsx` as a server component with metadata and placeholder content.

    Dennis must replace the placeholder body with content generated at termsfeed.com using:
    - Company name: Fort Property Developments
    - Email: Dennis@fortpropertydevelopment.com
    - Website: www.FortPropertyDevelopments.com
    - Jurisdiction: British Columbia (PIPA)

    ```typescript
    import type { Metadata } from 'next'

    export const metadata: Metadata = {
      title: 'Privacy Policy',
      description: 'Privacy Policy for Fort Property Developments — how we collect, use, and protect your personal information under PIPA BC.',
    }

    export default function PrivacyPage() {
      return (
        <main className="max-w-3xl mx-auto px-4 sm:px-6 lg:px-8 py-16">
          <h1 className="font-serif text-3xl text-fort-charcoal mb-2">Privacy Policy</h1>
          <p className="text-sm text-gray-500 mb-8">Fort Property Developments — British Columbia</p>

          {/* TODO: Replace this placeholder with content generated at termsfeed.com
              Required inputs: Company = Fort Property Developments,
              Email = Dennis@fortpropertydevelopment.com,
              Website = www.FortPropertyDevelopments.com, Jurisdiction = BC (PIPA) */}
          <div className="prose prose-sm max-w-none text-fort-gray space-y-6">
            <section>
              <h2 className="font-serif text-xl text-fort-charcoal mb-3">Information We Collect</h2>
              <p>
                Fort Property Developments collects personal information you voluntarily provide when
                you contact us or register interest in our developments. This includes your name,
                email address, phone number, and any message you submit through our website forms.
              </p>
            </section>

            <section>
              <h2 className="font-serif text-xl text-fort-charcoal mb-3">How We Use Your Information</h2>
              <p>
                We use the information you provide solely to respond to your inquiry, send you updates
                about developments you have expressed interest in, and fulfill the purpose for which
                you submitted the form. We do not sell or share your personal information with third parties.
              </p>
            </section>

            <section>
              <h2 className="font-serif text-xl text-fort-charcoal mb-3">Data Storage</h2>
              <p>
                Form submissions are processed through Formspree (formspree.io) and delivered to our
                email. We retain inquiry data only as long as necessary to respond to your request.
              </p>
            </section>

            <section>
              <h2 className="font-serif text-xl text-fort-charcoal mb-3">Your Rights Under PIPA BC</h2>
              <p>
                Under the Personal Information Protection Act (PIPA) of British Columbia, you have the
                right to request access to, correction of, or deletion of your personal information.
                To exercise these rights, contact us at{' '}
                <a href="mailto:Dennis@fortpropertydevelopment.com" className="text-fort-gold hover:underline">
                  Dennis@fortpropertydevelopment.com
                </a>.
              </p>
            </section>

            <section>
              <h2 className="font-serif text-xl text-fort-charcoal mb-3">Contact</h2>
              <p>
                For privacy-related questions, contact Fort Property Developments at{' '}
                <a href="mailto:Dennis@fortpropertydevelopment.com" className="text-fort-gold hover:underline">
                  Dennis@fortpropertydevelopment.com
                </a>{' '}
                or call{' '}
                <a href="tel:+16042906046" className="text-fort-gold hover:underline">
                  604-290-6046
                </a>.
              </p>
            </section>

            <p className="text-xs text-gray-400 pt-4 border-t border-gray-200">
              Last updated: {new Date().getFullYear()}. For a comprehensive policy, replace this content with TermsFeed-generated output.
            </p>
          </div>
        </main>
      )
    }
    ```

    Note: The `prose` Tailwind class requires `@tailwindcss/typography` plugin. If build fails with unknown class error, replace `className="prose prose-sm max-w-none text-fort-gray space-y-6"` with `className="text-sm text-fort-gray space-y-6 leading-relaxed"` — no plugin required.
  </action>
  <verify>
    <automated>cd /c/Users/denni/.notebooklm-mcp-cli && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>Build passes. app/privacy/page.tsx exists. Route /privacy is accessible (confirmed in sitemap.xml output or by running dev server).</done>
</task>

<task type="auto">
  <name>Task 2: Generate favicon.ico and icon.png</name>
  <files>app/favicon.ico, app/icon.png</files>
  <action>
    Attempt to generate favicon files from the existing SVG logo using available tools.

    **Step 1: Check available tools**
    ```bash
    which convert 2>/dev/null && echo "ImageMagick available" || echo "no ImageMagick"
    which inkscape 2>/dev/null && echo "Inkscape available" || echo "no Inkscape"
    node -e "require('sharp')" 2>/dev/null && echo "sharp available" || echo "no sharp"
    ```

    **Step 2a: If sharp is available (most likely on this system since Next.js uses it):**
    ```bash
    node -e "
    const sharp = require('sharp');
    // Generate 192px PNG
    sharp('public/logo/fort-logo-primary.svg')
      .resize(192, 192)
      .png()
      .toFile('app/icon.png')
      .then(() => console.log('icon.png created'));
    // Generate 32px ICO (as PNG, rename)
    sharp('public/logo/fort-logo-primary.svg')
      .resize(32, 32)
      .png()
      .toFile('app/favicon.ico')
      .then(() => console.log('favicon.ico created'));
    "
    ```

    Note: sharp generates PNG format even when named .ico. For a proper multi-size .ico,
    the PNG named favicon.ico still works in most browsers. Next.js App Router accepts it.

    **Step 2b: If ImageMagick is available:**
    ```bash
    convert -background none -resize 32x32 public/logo/fort-logo-primary.svg app/favicon.ico
    convert -background none -resize 192x192 public/logo/fort-logo-primary.svg app/icon.png
    ```

    **Step 2c: If no tools available — create placeholder text files with clear instructions:**
    Create `app/favicon.ico` as a text file with content:
    ```
    PLACEHOLDER — Replace with real favicon.ico
    Generate at: https://favicon.io/favicon-converter/
    Source: public/logo/fort-logo-primary.svg
    Size: 32x32 pixels
    ```
    Create `app/icon.png` as a text file with content:
    ```
    PLACEHOLDER — Replace with real icon.png
    Generate at: https://favicon.io/favicon-converter/
    Source: public/logo/fort-logo-primary.svg
    Size: 192x192 pixels
    ```

    For the placeholder approach: leave a note in SUMMARY.md that Dennis must replace these
    placeholder text files with real binary files before launch.

    Verify app/icon.svg still exists (Next.js auto-serves it as SVG favicon — do not overwrite):
    ```bash
    ls app/icon.svg
    ```
  </action>
  <verify>
    <automated>cd /c/Users/denni/.notebooklm-mcp-cli && ls app/favicon.ico app/icon.png app/icon.svg 2>&1</automated>
  </verify>
  <done>app/favicon.ico, app/icon.png, and app/icon.svg all exist. Build passes. Note in SUMMARY whether these are real binary exports or placeholders pending Dennis's manual export step.</done>
</task>

</tasks>

<verification>
Run: `npm run build`

Expected: Build exits 0. Route /privacy is included in generated public/sitemap.xml. All three favicon files exist in app/.

Manual check: Start dev server and visit /privacy — page renders with h1 "Privacy Policy". Visit any page — favicon visible in browser tab.
</verification>

<success_criteria>
- `npm run build` exits 0
- app/privacy/page.tsx exists with h1 and readable content
- /privacy route appears in public/sitemap.xml after build
- app/favicon.ico exists (real binary or documented placeholder)
- app/icon.png exists (real binary or documented placeholder)
- Footer Privacy Policy link at /privacy works (no 404)
</success_criteria>

<output>
After completion, create `.planning/phases/04-polish-and-launch/04-03-SUMMARY.md`
</output>
