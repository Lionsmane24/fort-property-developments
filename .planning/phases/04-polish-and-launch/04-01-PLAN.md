---
phase: 04-polish-and-launch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - next-sitemap.config.js
autonomous: true
requirements: [REQ-008, REQ-010]

must_haves:
  truths:
    - "npm run build succeeds and triggers next-sitemap automatically"
    - "sitemap.xml is generated in /public after build"
    - "robots.txt is generated in /public after build"
    - "next-plausible package is available for import in layout.tsx"
  artifacts:
    - path: "next-sitemap.config.js"
      provides: "Sitemap config with correct siteUrl and robots.txt generation"
      contains: "siteUrl: 'https://www.FortPropertyDevelopments.com'"
    - path: "package.json"
      provides: "postbuild script and new dev dependencies"
      contains: "postbuild"
  key_links:
    - from: "package.json postbuild"
      to: "next-sitemap binary"
      via: "npm postbuild lifecycle hook"
      pattern: "postbuild.*next-sitemap"
---

<objective>
Install next-sitemap and next-plausible packages, add postbuild script to package.json, and write next-sitemap.config.js. This Wave 1 task gates all other plans — nothing that depends on these packages can run until this completes.

Purpose: Unblocks plans 02, 03, and 04. Every subsequent build will automatically generate sitemap.xml and robots.txt.
Output: Two packages installed, postbuild hook wired, sitemap config created, build verified green.
</objective>

<execution_context>
@C:/Users/denni/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/denni/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install packages and add postbuild script</name>
  <files>package.json</files>
  <action>
    Run from project root:
    ```
    npm install next-plausible
    npm install next-sitemap --save-dev
    ```

    Then edit package.json scripts section to add the postbuild hook. The scripts section must become:
    ```json
    "scripts": {
      "dev": "next dev",
      "build": "next build",
      "start": "next start",
      "lint": "eslint",
      "postbuild": "next-sitemap"
    }
    ```

    npm `postbuild` runs automatically after `npm run build` completes — no manual invocation needed.
  </action>
  <verify>
    <automated>cd /c/Users/denni/.notebooklm-mcp-cli && grep "postbuild" package.json && grep "next-plausible" package.json && grep "next-sitemap" package.json</automated>
  </verify>
  <done>package.json has postbuild script, next-plausible in dependencies, next-sitemap in devDependencies</done>
</task>

<task type="auto">
  <name>Task 2: Write next-sitemap.config.js</name>
  <files>next-sitemap.config.js</files>
  <action>
    Create `next-sitemap.config.js` at the project root (same level as package.json, next.config.js):

    ```javascript
    /** @type {import('next-sitemap').IConfig} */
    module.exports = {
      siteUrl: 'https://www.FortPropertyDevelopments.com',
      generateRobotsTxt: true,
      // All 5 routes indexed: /, /about, /projects, /contact, /privacy
      // No exclusions for v1
    }
    ```

    Then run a full build to verify sitemap and robots.txt generate correctly:
    ```
    npm run build
    ```

    After build, confirm these files were created:
    - `public/sitemap.xml`
    - `public/robots.txt`

    Both files should list `https://www.FortPropertyDevelopments.com` as the base URL.
  </action>
  <verify>
    <automated>cd /c/Users/denni/.notebooklm-mcp-cli && npm run build && cat public/sitemap.xml | grep "FortPropertyDevelopments" && cat public/robots.txt | grep "FortPropertyDevelopments"</automated>
  </verify>
  <done>Build passes, public/sitemap.xml and public/robots.txt exist and contain the correct domain URL</done>
</task>

</tasks>

<verification>
Run: `npm run build && cat public/sitemap.xml && cat public/robots.txt`

Expected: Build green, sitemap.xml contains all 5 routes (/, /about, /projects, /contact, /privacy), robots.txt references sitemap URL.
</verification>

<success_criteria>
- `npm run build` exits 0
- `public/sitemap.xml` exists and contains `FortPropertyDevelopments.com`
- `public/robots.txt` exists and contains sitemap URL
- `next-plausible` importable (visible in node_modules)
</success_criteria>

<output>
After completion, create `.planning/phases/04-polish-and-launch/04-01-SUMMARY.md`
</output>
