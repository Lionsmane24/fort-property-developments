---
phase: 03-page-build
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/Nav.tsx
  - components/Footer.tsx
  - public/images/hero-bg.jpg
autonomous: true
requirements: [REQ-004, REQ-005, REQ-006, REQ-007]

must_haves:
  truths:
    - "Nav CTA reads 'Register Interest' and links to /contact"
    - "Footer shows Dennis@fortpropertydevelopment.com and 604-290-6046"
    - "Hero background image exists at public/images/hero-bg.jpg under 300KB"
  artifacts:
    - path: "components/Nav.tsx"
      provides: "Sticky nav with corrected CTA"
      contains: "Register Interest"
    - path: "components/Footer.tsx"
      provides: "Footer with real contact info"
      contains: "Dennis@fortpropertydevelopment.com"
    - path: "public/images/hero-bg.jpg"
      provides: "Hero background image"
      min_size: "10KB"
  key_links:
    - from: "components/Nav.tsx"
      to: "/contact"
      via: "CTA href prop"
      pattern: "href.*contact"
    - from: "components/Footer.tsx"
      to: "Dennis@fortpropertydevelopment.com"
      via: "anchor tag"
      pattern: "Dennis@fortpropertydevelopment"
---

<objective>
Fix two Phase 2 carryover issues (Nav CTA broken anchor, Footer placeholder contact info) and add the hero background image asset. This plan unblocks all four page builds in Wave 2.

Purpose: The Nav "Book a Call" CTA links to `#booking` which does not exist on any page — clicking it scrolls to nothing. The Footer still uses placeholder values from Phase 2. Both must be corrected before the pages can be reviewed. The hero image must exist locally because `next/image` cannot fetch remote Unsplash URLs without domain allowlisting.

Output: Corrected Nav.tsx, corrected Footer.tsx, `public/images/hero-bg.jpg` on disk.
</objective>

<execution_context>
@C:/Users/denni/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/denni/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@components/Nav.tsx
@components/Footer.tsx
</context>

<interfaces>
<!-- Key patterns already established in this codebase. Use exactly. -->

Color tokens (from tailwind.config.js — use these, never raw hex):
  fort-charcoal, fort-gold, fort-bg, fort-gray

Button component:
  <Button href="/contact">Register Interest</Button>
  <Button href="/contact" variant="secondary">Register Interest</Button>

Logo files:
  /public/logo/fort-logo-primary.svg   — for light backgrounds
  /public/logo/fort-logo-reversed.svg  — for dark backgrounds (Nav uses this)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fix Nav CTA — "Register Interest" linking to /contact</name>
  <files>components/Nav.tsx</files>
  <action>
Read components/Nav.tsx. Find the CTA button/link that currently reads "Book a Call" and links to `#booking`. Change:
- Text: "Book a Call" → "Register Interest"
- href: `#booking` → `/contact`

Make only this targeted change. Do not alter any other Nav logic (sticky behavior, mobile overlay, scroll shadow, hamburger). The Nav is otherwise complete and verified from Phase 2.
  </action>
  <verify>
    <automated>cd /c/Users/denni/.notebooklm-mcp-cli && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>"Register Interest" appears in Nav.tsx with href="/contact". Build passes with no TypeScript errors.</done>
</task>

<task type="auto">
  <name>Task 2: Update Footer with real contact information</name>
  <files>components/Footer.tsx</files>
  <action>
Read components/Footer.tsx. Replace the two placeholder values:
- `info@fortpropertydev.com` → `Dennis@fortpropertydevelopment.com`
- `604-555-0000` → `604-290-6046`

Update both the display text and the href values:
- Email anchor: `href="mailto:Dennis@fortpropertydevelopment.com"`
- Phone anchor: `href="tel:+16042906046"`

Make only these targeted replacements. Do not alter layout, logo, nav links, copyright year, or any other Footer content.
  </action>
  <verify>
    <automated>cd /c/Users/denni/.notebooklm-mcp-cli && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Footer.tsx contains Dennis@fortpropertydevelopment.com and 604-290-6046 in both href and display text. Build passes.</done>
</task>

<task type="auto">
  <name>Task 3: Download hero background image to public/images/</name>
  <files>public/images/hero-bg.jpg</files>
  <action>
Create the `public/images/` directory if it does not exist.

Download a suitable architectural hero image from Unsplash. Target: modern residential architecture, urban, clean lines — matches a Metro Vancouver multiplex developer brand. Suggested search: "modern residential architecture" or "contemporary urban housing".

Requirements:
- File: `public/images/hero-bg.jpg`
- Format: JPEG
- Size: under 300KB (REQ-010 constraint)
- Dimensions: at least 1920px wide for full-bleed quality
- Content: architectural subject, no people faces (avoids rights issues), no obvious non-BC landmark

Use curl to download directly from a free Unsplash source URL, or use a publicly available architectural stock photo. Example command approach:
  mkdir -p public/images
  curl -L "https://images.unsplash.com/photo-1545324418-cc1a3fa10c00?w=1920&q=80&fm=jpg" -o public/images/hero-bg.jpg

Verify the file is under 300KB after download. If the downloaded file exceeds 300KB, use ImageMagick (if available) or download at a lower quality parameter (q=60).

Note: The image is served via next/image from the local path "/images/hero-bg.jpg" — no domain allowlisting needed for local files.
  </action>
  <verify>
    <automated>ls -lh /c/Users/denni/.notebooklm-mcp-cli/public/images/hero-bg.jpg && cd /c/Users/denni/.notebooklm-mcp-cli && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>public/images/hero-bg.jpg exists, is under 300KB, and build passes with no errors.</done>
</task>

</tasks>

<verification>
After all tasks:
- `npm run build` passes with no TypeScript or import errors
- Nav CTA reads "Register Interest" and href="/contact"
- Footer shows Dennis@fortpropertydevelopment.com (href + text) and 604-290-6046 (href + text)
- `public/images/hero-bg.jpg` exists and is under 300KB
</verification>

<success_criteria>
- Nav "Book a Call" → "Register Interest" with correct href (broken anchor eliminated)
- Footer placeholder contacts replaced with real values
- Hero image on disk and ready for next/image consumption
- All Wave 2 plans (02–05) can proceed in parallel
</success_criteria>

<output>
After completion, create `.planning/phases/03-page-build/03-01-SUMMARY.md`
</output>
