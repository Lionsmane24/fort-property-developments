---
phase: 03-page-build
plan: 06
type: execute
wave: 3
depends_on: [03-02, 03-03, 03-04, 03-05]
files_modified: []
autonomous: false
requirements: [REQ-004, REQ-005, REQ-006, REQ-007]

must_haves:
  truths:
    - "All 4 pages load at their routes without error"
    - "Hero image is visible with appropriate height on the Home page"
    - "Contact and Register Interest forms show fallback when endpoint is placeholder"
    - "All pages are mobile-responsive at 375px with no horizontal scroll"
    - "Nav CTA reads Register Interest and links to /contact"
    - "Footer shows real email and phone number"
  artifacts:
    - path: "app/page.tsx"
      provides: "Home page"
    - path: "app/about/page.tsx"
      provides: "About page"
    - path: "app/projects/page.tsx"
      provides: "Projects page"
    - path: "app/contact/page.tsx"
      provides: "Contact page"
  key_links:
    - from: "localhost:3000"
      to: "all 4 page routes"
      via: "Next.js App Router"
      pattern: "/, /about, /projects, /contact"
---

<objective>
Human verification checkpoint: run the dev server and confirm all 4 pages render correctly across desktop and mobile viewports. This is the final gate before Phase 3 is marked complete.

Purpose: Build passes confirm TypeScript correctness but cannot verify visual rendering, image height, form fallback behavior, or mobile layout. A human review catches these before Phase 4 begins.

Output: Human approval that all 4 pages are working as designed.
</objective>

<execution_context>
@C:/Users/denni/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/denni/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Final build and lint validation</name>
  <files></files>
  <action>
Run the full validation suite before asking for human review:

```bash
cd /c/Users/denni/.notebooklm-mcp-cli
npm run build && npm run lint
```

If build fails: read the error output, identify the failing file and line, fix the issue, and re-run. Do not proceed to the checkpoint until both commands pass.

If lint returns warnings (not errors): note them but proceed — warnings don't block the checkpoint.
  </action>
  <verify>
    <automated>cd /c/Users/denni/.notebooklm-mcp-cli && npm run build && npm run lint 2>&1 | tail -15</automated>
  </verify>
  <done>npm run build AND npm run lint both exit 0 (no errors). Phase 3 pages are TypeScript-clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
All 4 Phase 3 pages are now built:
- Home page (app/page.tsx) — hero, value strip, projects preview, about teaser, trust bar, CTA banner
- About page (app/about/page.tsx) — founder bio, 3 values, Fort Difference, service area, affiliations, CTA
- Projects page (app/projects/page.tsx) — Burnaby Multiplex card, Register Interest inline form, pre-launch empty state
- Contact page (app/contact/page.tsx) — split layout, info block with real contact details, Formspree form with fallback

Plus:
- Nav CTA updated to "Register Interest" linking to /contact
- Footer updated with Dennis@fortpropertydevelopment.com and 604-290-6046
  </what-built>
  <how-to-verify>
1. Run: `npm run dev` in the project directory
2. Visit http://localhost:3000 — verify:
   - Hero image fills the viewport with dark overlay, H1 is readable
   - "View Our Projects" and "Register Interest" buttons are visible
   - Value strip shows 3 columns below hero
   - Nav CTA reads "Register Interest" (not "Book a Call")
3. Visit http://localhost:3000/about — verify:
   - Page loads with charcoal hero header and H1 "About Fort Property"
   - Founder section shows placeholder with draft bio paragraphs
   - 3 company values cards are visible
4. Visit http://localhost:3000/projects — verify:
   - Burnaby Multiplex card visible with "Planning" badge
   - Register Interest inline form shows the mailto fallback (since endpoint is still xxx)
   - Pre-launch empty state visible below the card
5. Visit http://localhost:3000/contact — verify:
   - Split layout: info block on left, form area on right (on desktop)
   - Dennis@fortpropertydevelopment.com is visible and clickable
   - 604-290-6046 is visible and clickable
   - Form area shows the mailto fallback (since endpoint is still xxx)
   - "Responds within 1 business day" note is visible
6. Mobile check — open DevTools → toggle device mode → iPhone SE (375px):
   - All 4 pages: no horizontal scroll, content readable, buttons accessible
7. Check footer on any page: confirm Dennis@fortpropertydevelopment.com and 604-290-6046 appear
  </how-to-verify>
  <resume-signal>
Type "approved" if all pages look correct.
Or describe specific issues (e.g., "hero image not showing", "about page missing values section") and Claude will fix them before re-verification.
  </resume-signal>
</task>

</tasks>

<verification>
All verifications are manual browser checks listed in the checkpoint above.
The automated gate is Task 1: `npm run build && npm run lint` must pass before human review begins.
</verification>

<success_criteria>
- Human approves all 4 pages render correctly at desktop and mobile
- No horizontal scroll at 375px on any page
- Hero image visible with correct height
- Form fallbacks working (mailto shown since endpoint=xxx)
- Real contact info (email + phone) visible as text on contact page and footer
- Phase 3 ready for Phase 4: Polish and Launch
</success_criteria>

<output>
After approval, create `.planning/phases/03-page-build/03-06-SUMMARY.md`

Also update STATE.md:
- current_phase: 3 — Page Build
- current_plan: complete
- status: complete (pending Phase 4)
- Document any issues found during review as Key Decisions for Phase 4
</output>
