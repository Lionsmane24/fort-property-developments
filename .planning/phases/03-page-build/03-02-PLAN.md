---
phase: 03-page-build
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - app/page.tsx
autonomous: true
requirements: [REQ-004]

must_haves:
  truths:
    - "Home page hero renders full-viewport with image, dark overlay, H1, and two CTAs"
    - "Value strip shows 3 columns: Experience, Location, Quality"
    - "Projects preview section shows at least one project card placeholder"
    - "About teaser section has photo placeholder and paragraph with Learn More CTA"
    - "Trust bar shows styled affiliation badges (CHBA, HAVAN, UDI)"
    - "CTA banner at bottom captures interest with a button linking to /contact"
  artifacts:
    - path: "app/page.tsx"
      provides: "Complete Home page with all 6 sections"
      contains: "Building Metro Vancouver"
  key_links:
    - from: "app/page.tsx"
      to: "components/Button.tsx"
      via: "import"
      pattern: "import.*Button"
    - from: "app/page.tsx"
      to: "components/Section.tsx"
      via: "import"
      pattern: "import.*Section"
    - from: "app/page.tsx"
      to: "public/images/hero-bg.jpg"
      via: "next/image src prop"
      pattern: "hero-bg"
---

<objective>
Replace the placeholder `app/page.tsx` with the complete Home page. Implements all 6 sections from REQ-004: hero, value strip, projects preview, about teaser, trust bar, and CTA banner.

Purpose: The Home page is the primary landing experience for investors and buyers. All sections must render with appropriate copy and visual hierarchy using established Fort brand tokens.

Output: `app/page.tsx` — complete server component with all 6 sections.
</objective>

<execution_context>
@C:/Users/denni/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/denni/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-page-build/03-01-SUMMARY.md
@.planning/ROADMAP.md
@components/Button.tsx
@components/Section.tsx
@components/animations/FadeIn.tsx
@components/animations/FadeInGroup.tsx
</context>

<interfaces>
<!-- Phase 2 component contracts — use exactly as shown. -->

Section component:
```typescript
// Applies py-16 md:py-24 and max-w-6xl container automatically
<Section id="optional-anchor" className="bg-white">
  {children}
</Section>
```

Button component:
```typescript
// Renders as <a> when href provided, <button> otherwise
<Button href="/projects">View Our Projects</Button>              // primary gold fill
<Button href="/contact" variant="secondary">Register Interest</Button>  // gold outline
```

FadeIn component:
```typescript
// Viewport-triggered opacity fade. delay in ms.
<FadeIn delay={0}>{children}</FadeIn>
<FadeIn delay={100}>{children}</FadeIn>
```

FadeInGroup component:
```typescript
// 100ms stagger. Requires array of children, not a single child.
<FadeInGroup>{[<div key="a">...</div>, <div key="b">...</div>]}</FadeInGroup>
```

Color tokens (Tailwind classes — never raw hex):
  fort-charcoal, fort-gold, fort-bg, fort-gray

Font classes:
  font-serif  → Playfair Display (all H1–H3)
  font-sans   → Inter (body, labels, nav)

Logo:
  /public/logo/fort-logo-reversed.svg  — use on dark backgrounds
  /public/logo/fort-logo-primary.svg   — use on light backgrounds
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Build Home page — hero + value strip sections</name>
  <files>app/page.tsx</files>
  <action>
Replace `app/page.tsx` with a server component (no `'use client'` directive). The file is currently a placeholder stub.

**Section 1: Hero**
- Outer: `<section className="relative w-full h-[90vh] min-h-[600px] flex items-end md:items-center pt-16 md:pt-20">`
  - `pt-16 md:pt-20` compensates for the fixed Nav height so content isn't hidden behind it
- Background: `<Image src="/images/hero-bg.jpg" alt="Modern multiplex development in Metro Vancouver" fill priority className="object-cover object-center" sizes="100vw" />`
- Dark overlay: `<div className="absolute inset-0 bg-fort-charcoal/70" />`
- Content (above overlay, `relative z-10`):
  - `<h1 className="font-serif text-4xl md:text-6xl text-white leading-tight max-w-3xl">Building Metro Vancouver's Next Generation of Homes</h1>`
  - `<p className="font-sans text-gray-300 mt-4 max-w-xl text-lg">Fort Property Developments builds 4–6 unit multiplex infill across Metro Vancouver and the Fraser Valley — density done right.</p>`
  - Button row: `<Button href="/projects">View Our Projects</Button>` and `<Button href="/contact" variant="secondary">Register Interest</Button>` with `flex gap-4 mt-8 flex-wrap`

**Section 2: Value strip**
- `<Section className="bg-white">`
- 3-column grid: `grid md:grid-cols-3 gap-8 text-center`
- Each column: icon (inline SVG, ~40px), bold heading (`font-serif text-xl text-fort-charcoal`), body text (`font-sans text-fort-gray text-sm mt-2`)
- Column content:
  1. Experience: A simple house/building SVG outline + "Built for BC" + "Deep roots in Metro Vancouver and Fraser Valley real estate, with a focus on missing middle housing."
  2. Location: A map pin SVG + "Metro Vancouver Focus" + "Burnaby, Surrey, North Vancouver, Coquitlam, and the Fraser Valley — where density is needed most."
  3. Quality: A shield/checkmark SVG + "Built to Last" + "Every Fort development is engineered for long-term performance — materials, systems, and craftsmanship."
- Wrap columns in `<FadeInGroup>` for stagger

Use simple inline SVG paths for icons (24x24 viewBox, stroke-based, `stroke-fort-gold`). Keep them minimal — 2-3 path elements max.
  </action>
  <verify>
    <automated>cd /c/Users/denni/.notebooklm-mcp-cli && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>app/page.tsx compiles with no TypeScript errors. Hero and value strip sections are fully defined.</done>
</task>

<task type="auto">
  <name>Task 2: Build Home page — projects preview, about teaser, trust bar, CTA banner</name>
  <files>app/page.tsx</files>
  <action>
Continue building `app/page.tsx` — add sections 3 through 6 after the value strip.

**Section 3: Projects preview**
- `<Section id="projects" className="bg-fort-bg">`
- Heading: `<h2 className="font-serif text-3xl md:text-4xl text-fort-charcoal">Our Developments</h2>`
- Subheading: `<p className="font-sans text-fort-gray mt-2">BC's next generation of missing middle housing.</p>`
- Project card grid: `grid md:grid-cols-2 lg:grid-cols-3 gap-6 mt-10`
- One real card for "Burnaby Multiplex":
  - Placeholder image area: `<div className="h-48 bg-fort-charcoal/10 rounded-t-lg flex items-center justify-center"><span className="font-sans text-fort-gray text-sm">Render coming soon</span></div>`
  - Card body: status badge (`inline-block px-2 py-1 rounded text-xs font-semibold bg-amber-100 text-amber-800`, text "Planning"), h3 `font-serif text-xl`, type + location `font-sans text-fort-gray text-sm`
  - Card data: name="Burnaby Multiplex", type="4-unit infill", location="Burnaby, BC"
  - CTA: `<Button href="/projects" variant="secondary" className="mt-4">Register Interest</Button>` per BCFSA — never "reserve" or "deposit"
- Below grid: "More projects coming soon" note in `text-fort-gray font-sans text-sm text-center mt-6`
- Wrap section body in `<FadeIn>`

**Section 4: About teaser**
- `<Section className="bg-white">`
- 2-column layout: `grid md:grid-cols-2 gap-12 items-center`
- Left: founder photo placeholder — `<div className="w-full h-72 md:h-96 bg-fort-charcoal/10 rounded-lg flex items-center justify-center"><span className="font-sans text-fort-gray text-sm">Founder photo coming soon</span></div>`
- Right:
  - `<h2 className="font-serif text-3xl text-fort-charcoal">Fort's Foundation</h2>`
  - `<p className="font-sans text-fort-gray mt-4 leading-relaxed">Dennis founded Fort Property Developments to bring thoughtful, high-quality multiplex infill to Metro Vancouver communities. With BC roots and a focus on the missing middle, Fort builds homes that strengthen neighbourhoods for the long term.</p>`
  - `<Button href="/about" variant="secondary" className="mt-6">Learn More</Button>`
- Wrap in `<FadeIn>`

**Section 5: Trust bar**
- `<Section className="bg-fort-bg">`
- Centered: `<p className="font-sans text-fort-gray text-sm uppercase tracking-widest text-center mb-6">Industry Affiliations</p>`
- Affiliation badges row: `flex flex-wrap justify-center gap-6`
- Each badge: `<div className="px-6 py-3 border border-fort-gray/30 rounded font-sans text-fort-charcoal font-semibold text-sm">CHBA Member</div>` — repeat for "HAVAN Member" and "UDI BC"
- Note in code comment: `{/* TODO: Replace text badges with real affiliation logos when provided */}`

**Section 6: CTA banner**
- Dark full-width section (not inside `<Section>`): `<section className="bg-fort-charcoal py-16 md:py-20">`
- Inner container: `<div className="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 text-center">`
- `<h2 className="font-serif text-3xl md:text-4xl text-white">Stay Ahead of the Market</h2>`
- `<p className="font-sans text-gray-300 mt-4 max-w-xl mx-auto">Be the first to hear about new Fort developments. Register your interest today — no commitment, no deposit.</p>`
- `<Button href="/contact" className="mt-8">Register Interest</Button>` (primary gold on dark)

All sections import: `import Image from 'next/image'`, `import { Button } from '@/components/Button'`, `import { Section } from '@/components/Section'`, `import { FadeIn } from '@/components/animations/FadeIn'`, `import { FadeInGroup } from '@/components/animations/FadeInGroup'`
  </action>
  <verify>
    <automated>cd /c/Users/denni/.notebooklm-mcp-cli && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>All 6 Home page sections are in app/page.tsx. Build passes with no TypeScript errors. File contains all required sections: hero, value strip, projects preview, about teaser, trust bar, CTA banner.</done>
</task>

</tasks>

<verification>
- `npm run build` passes with no errors
- `app/page.tsx` is a default export server component (no `'use client'`)
- Hero section uses `next/image` with `fill` and `priority`, parent has `relative` and explicit height
- All CTAs say "Register Interest" or "View Our Projects" — never "reserve" or "deposit"
- `pt-16 md:pt-20` on first section prevents content hiding behind fixed Nav
</verification>

<success_criteria>
- Home page compiles and all 6 required sections are present
- REQ-004 fully satisfied: hero, value strip, projects preview, about teaser, trust bar, CTA banner
- No BCFSA-violating language in any CTA
- Build green
</success_criteria>

<output>
After completion, create `.planning/phases/03-page-build/03-02-SUMMARY.md`
</output>
